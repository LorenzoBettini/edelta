// IMPORTANT: ecores must be in source directories
// otherwise you can't refer to them

metamodel "ecore"
metamodel "myecore"

def createANewClassInMyEcore(String name) {
	ecoreref(myecore).addNewEClass(name)
}

